import { createContext } from "react";

export const ContextInside = createContext();

export const ContextInsideProvider = ContextInside.Provider;
export const ContextInsideConsumer = ContextInside.Consumer;
